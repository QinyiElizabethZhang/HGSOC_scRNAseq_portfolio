# Raw data

Raw public scRNA-seq data should be downloaded separately by the user.

This folder is intentionally excluded from GitHub using `.gitignore`.

Dataset source and download instructions are documented in the main README once the dataset is selected.

